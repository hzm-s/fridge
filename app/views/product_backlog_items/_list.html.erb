<%= content_tag :div, id: "section0", class: 'pbl__list', data: is_sortable ? sortable_pbl_options(product_id).merge(release: 1) : {} do %>
  <%= render partial: 'item', collection: items, locals: { is_sortable: is_sortable } %>
<% end %>
