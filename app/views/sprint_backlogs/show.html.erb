<% content_for(:member_roles) { team_member_roles_label } %>
<div class="row d-flex justify-content-center" test-sprint-backlog-<%= current_sprint.id %>="1">
  <div class="col-lg-12 sheet">
    <div class="d-flex justify-content-center">
      <h1 class="page-title">
        <%=t '.title' %>
      </h1>
    </div>
    <%= render partial: 'list', locals: { items: @sbl.issues } %>
  </div>
</div>
<div class="row d-flex justify-content-center">
  <div class="d-flex mt-2">
    <%= link_to products_path, class: 'text-md' do %>
      <i class="fas fa-reply"></i>
      <%=t 'navs.back' %>
    <% end %>
  </div>
</div>
