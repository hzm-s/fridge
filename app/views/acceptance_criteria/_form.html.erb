<%= form_with scope: :form, url: pbi_acceptance_criteria_path(pbi_id: pbi_id), id: 'js-ac-form', data: { test_append_acceptance_criterion: 1 } do |f| %>
  <%= f.text_field :content, class: 'form-control', autocomplete: 'off' %>
  <div class="mt-2">
    <%= f.button t('.add'), class: 'btn btn-success', data: with_loader %>
  </div>
<% end %>
