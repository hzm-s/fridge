<div class="ac-list__item">
  <p>
    <%= criterion.content %>
  </p>
  <% if editable %>
    <%= link_to issue_acceptance_criterion_path(issue_id: issue_id, number: criterion.number), method: :delete, class: 'silent-danger ml-3', data: { confirm: t('navs.confirm_delete') } do %>
      <i class="fas fa-trash-alt"></i>
    <% end %>
  <% end %>
</div>
